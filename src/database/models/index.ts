/**
 * Export all WatermelonDB models
 */

import Transaction from './Transaction';
import Category from './Category';
import Account from './Account';

export { Transaction, Category, Account };

export default [Transaction, Category, Account];
